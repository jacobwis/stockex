import * as React from 'react';
import Card from './Card';

const NavBar: React.StatelessComponent = () => {
  return <Card className="NavBar">{/* <h1>NavBar</h1> */}</Card>;
};

export default NavBar;
